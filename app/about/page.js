// component name doesn't matter as it route with folder name.but again only with  folder,we can't route,so we need page.js for routing(add content here)
//So now this route path would be like  my-page.com/about
export default  function AboutPage() {
    return(
    <main>
        <h1>About us</h1>
    </main>
)
}